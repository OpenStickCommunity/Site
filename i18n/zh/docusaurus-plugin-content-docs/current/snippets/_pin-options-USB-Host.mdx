- `D+` - 用于在 USB 主机端口和 RP2040 之间传输 Data Plus 的 GPIO 引脚。
- `D-` - 用于在 USB 主机端口和 RP2040 之间传输 Data Minus 的 GPIO 引脚。此引脚无法手动设置，将根据 `D+` 自动确定。
- `5V Power (可选)` - 用于为板上的主机端口启用 5V 电源的 GPIO 引脚。

:::note 5V 电源

这仅适用于少量板子（例如 Adafruit RP2040 Feather USB Host），大多数板子不需要设置此选项。

:::
